# calculate the area of rectangle ask the inputs to the user and print appropriately

len = float(input(" Enter the length : "))
wid = float(input("Enter the width : "))

area = len * wid 

print(f"The area of triangle with length = {len}cm and width = {wid}cm is : {area}sq.cm")